import React from 'react';


class Camera extends React.Component {

    // state = {
    //     tour_name: ""
    // }

    // handleChange = (event) => {
    //     this.setState({tour_name: event.target.value})
    // }


    render() {
        return (
            <div>insert camera here</div>
        )
    }
}

export default Camera;